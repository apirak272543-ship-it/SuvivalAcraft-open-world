# Blueprints

JSON blueprints for structures placeable in the world. `X` = wall, `.` = open floor, `D` = door, `F` = fire.

- `house_basic.json` — starter wooden house
- `campfire.json` — campfire foundation
